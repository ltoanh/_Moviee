import React from 'react';
import Navbar from './navbar';
import './header.scss';

function Header() {
	return (
		<header className="container">
			<Navbar />
		</header>
	);
}

export default Header;
